# Domain docs

Use a single-context layout: root CONTEXT.md and docs/adr/.

Before exploring code, read CONTEXT.md and relevant ADRs.
If absent, proceed silently; domain-modeling creates them as needed.
Use the glossary's terms and explicitly flag conflicts with existing ADRs.
