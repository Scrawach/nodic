# Продолжение Nodic v1

## Актуальное указание пользователя — 2026-09-15

**Оптимизация отложена бессрочно. Задачи 12 и 13 не исполнять.** По указанию пользователя от 2026-09-15 карточки сохранены только как справочный материал и исключены из активной очереди; приоритет P1 снят. Не исследовать, не планировать, не реализовывать, не публиковать их и не повторять нагрузочный профиль в рамках этих задач. Не предлагать их следующим шагом. Возобновление — только по новому явному указанию пользователя вернуться к оптимизации или соответствующей задаче.

Очередь 01–11 завершена. Следующая работа определяется новым запросом пользователя, без автоматического перехода к оптимизации. Измеренные ограничения остаются фактами, но сейчас не являются поручением к работе. Это указание имеет приоритет над прежними рекомендациями и фразами «следующая задача» в истории ниже.

## Завершённая задача 11 — 2026-09-15

- `App.tsx`, `style.css`: список персонажей с именем и цветом расположен над диалогами, использует то же оформление строк и кнопки добавления. Пустой список объяснён; длинные имена переносятся, длинный список прокручивается до диалогов и их кнопки добавления.
- `Characters.tsx`: общие формы создания и изменения имени/цвета/удаления используются отдельной страницей и окном доски. Выбор персонажа открывает его редактор, удаление требует подтверждения. Использованы существующие HTTP-операции и соединение доски, серверные права/транзакции не менялись. Удаление коллегой закрывает открытый редактор с уведомлением; офлайн изменения отключены.
- Новый публичный браузерный сценарий `sidebar characters can be created and edited` проверяет пустой список, расположение, создание, выбор, имя/цвет, изменения открытого редактора коллеги, отмену подтверждения, удаление редактором, закрытие обоих окон, offline/reconnect и перезагрузку. Три независимых контекста проверяют синхронизацию авторов и отдельный проект, включая отказ 403 на чужое создание. Сценарий с 14 персонажами и именем 115 знаков проверяет прокрутку и доступность добавления диалога.
- Визуально просмотрены снимки 1280×720: пустой список, имя/цвет, окно редактирования, подтверждение удаления, длинное имя и прокрутка до диалогов. Снимки создаются в `test-results/character-sidebar-*.png`. Оформление согласовано, пересечений/выхода длинного имени за панель нет.
- README, first-slice, карточка 11 и матрица v1 (срез, истории 10/17–19, остаток 5) обновлены. **v1 не принят**: 12/13, измеренный провал нагрузки, область Undo и пробелы доказательств из задачи 10 остаются открытыми. Профиль нагрузки не повторяли, GitHub не публиковали.
- Проверки: `npm run typecheck`, `npm test` (22/22), `npm run test:e2e` (22/22, Microsoft Edge), `npm run build` и `git diff --check` прошли. Сборка предупреждает о чанке 894.86 kB (279.51 kB gzip). Первый полный e2e выявил два неоднозначных старых локатора «Новый персонаж» после появления одноимённой кнопки sidebar; локаторы ограничены текущим окном, повторный полный запуск прошёл.
- Перед проверками прежние PostgreSQL/API/Vite оказались остановлены; тот же тестовый кластер `.task08-pg` перезапущен на 55432, Playwright поднял API/Vite. Для e2e также задан `NODE_OPTIONS=--require=./.scratch/task08-env.cjs`; остальные Windows-команды из раздела задачи 08 актуальны. Тестовое окружение оставлено работающим.

Очередь 08–11 завершена отдельными коммитами. Задачи [12](12.md)/[13](13.md) отложены бессрочно и не исполняются до нового явного указания пользователя.

## Завершённая задача 10 — 2026-09-15

- Создана [матрица всех 27 историй](../v1-verification.md): каждой сопоставлены актуальные файлы реализации, уникальные имена/начала имён публичных серверных и браузерных тестов, ограничения и непроверенные случаи. Это аудит, продуктовый код и тесты не менялись.
- README, first-slice, architecture и статусы спецификации больше не называют архитектуру/стек/форму варианта невыбранными; отражены готовые история текста/графа, IndexedDB, проверенные отказы и выполненный профиль нагрузки. Исходные 27 требований сохранены без сужения.
- **v1 не принят**: измеренный провал нагрузки остаётся за 12/13; широкая история 24 не полностью реализована (Undo не охватывает справочник и управление проектом/диалогами). Отдельно отмечены пробелы доказательств: положительный возврат из недавних, читаемость длинного варианта, успешные цикл/схождение после перезагрузки, навигация/миникарта и drag выделенной группы. Это не вновь обнаруженные поломки. Границы индикатора сохранения и повторов HTTP отделены от требований.
- Полный запуск: `npm run typecheck`, `npm test` (22/22), `npm run test:e2e` (21/21, Microsoft Edge), `npm run build` прошли. Сборка по-прежнему предупреждает о чанке 892.76 kB (279.19 kB gzip). Нагрузку повторно не запускали: приложение не менялось, в матрице ссылки на исходные результаты 09. Команды/окружение задачи 08 ниже остаются актуальны; общий PostgreSQL/API/Vite оставлены работающими.
- Публикации в GitHub не выполнялись и отклонённая публикация 12 не повторялась. Матрица/README/спецификация — локальные документы до отдельного решения о публикации.
- `git diff --check` прошёл; проверены наличие ровно 27 последовательных строк матрицы и существование локальных целей ссылок изменённых документов. Задача сохраняется отдельным коммитом.

Следующая задача: [11. Работа с персонажами в проекте](11.md). После её реализации обновить дату/срез матрицы, строки 10 и 17–19, остаток 5 и соответствующие пользовательские документы, сохранив открытые 12/13, расхождение Undo и пробелы проверки. Завершение 10 означает завершение аудита, не приёмку v1.

Ниже — история выполнения; старые фразы «следующая задача» и результаты проверок относятся к моменту записи соответствующего раздела. Актуальная передача — в верхнем разделе и очереди.

## Завершённая задача 09 — 2026-09-15

- Добавлен самостоятельный `npm run profile:load` (`scripts/profile-load.mjs`): production-доска, 1000 нод/998 связей, 10 независимых браузерных контекстов, 100 burst-перемещений, 100 Yjs-обновлений и 20 настоящих drag. Каждый ACK и доставка всем 9 коллегам проверены. Проект создаётся и удаляется только публичными операциями; таблицы не используются как утверждения.
- [Отчёт](../research/load-profile.md) содержит окружение, методику, ограничения, заранее выбранные бюджеты, два полных успешных запуска, исходные числовые выборки и снимок. Калибровочные ошибки harness исправлены и исключены из результатов.
- Повторяемые p95: загрузка 1.74–1.79 s; ACK движения 10.38–10.40 s; доставка 10.04–10.15 s; ACK текста 3.47–4.08 s; доставка текста 3.01–3.56 s; кадры 483 ms; heap 211–221 MiB. Полный graph ~632 KB на каждое перемещение каждому автору. Потерь ACK/доставки не было.
- **Производительность 1000/10 заблокирована**: загрузка проходит бюджет, отзывчивость и память нет. Созданы отдельные P1-карточки 12/13; они не реализуются в текущей очереди 08–11. Production-код в 09 не менялся.
- Проверки: `npm run typecheck`, `npm test` (22/22), `npm run test:e2e` (21/21, Edge), `npm run build` прошли. Сборка предупреждает о чанке 892.76 kB; холодная WAN-загрузка не измерялась. `git diff --check` без ошибок.
- Windows: запуск профильного API на 3009 через игнорируемый `.scratch/task09-server.cjs` с `tsx/esm/api`; `PROFILE_ORIGIN=http://127.0.0.1:3009`, Edge и системные переменные через `.scratch/task09-run.cjs`. Воспроизведение в обычной оболочке описано в отчёте без зависимости от этих временных файлов. Общие API/Vite/PostgreSQL задачи 08 остаются запущены; отдельный профильный API остановлен после работы.
- GitHub Issues доступны для чтения с эскалацией; открыта только #1. Создание issue из 12 отклонено автоматической проверкой разрешений как неавторизованная публикация локального документа. Повтор не выполнялся; нужны одобрение пользователя и публикация подготовленных 12/13 после завершения очереди. Локальная работа и коммит 09 этим не блокируются.

Следующая задача: [10. Сверка v1 и документации](10.md). Учитывать измеренное ограничение производительности, не объявлять весь v1 готовым. Затем 11; новые 12/13 оставить явными остатками.

Обновлено: 2026-09-15. Пользователь поручил выполнять оставшиеся задачи и сохранять возможность передачи другому агенту.

## Как продолжить

1. Прочитать корневой AGENTS.md, README, docs/first-slice.md, CONTEXT.md и ADR 0001.
2. Проверить git status/diff: незакоммиченные изменения могут быть предыдущей работой.
3. Открыть активную карточку ниже и продолжить с записанного шага.
4. После изменения статуса записать файлы, результаты проверок, ограничения и следующий конкретный шаг здесь. Закрывать задачу только после всех её критериев и проверок.

## Очередь

- Готово: [01. Надёжное создание объектов](01.md)
- Готово: [02. Персональная отмена структуры](02.md)
- Готово: [03. Единая история текста и структуры](03.md)
- Готово: [04. Копирование, вставка и групповое удаление](04.md)
- Готово: [05. Справочник персонажей](05.md)
- Готово: [06. Управление проектами и диалогами](06.md)
- Готово: [07. Присутствие авторов на доске](07.md)
- Готово: [08. Отказы и восстановление](08.md)
- Готово (измерение; performance gate не пройден): [09. Нагрузка 1 000 нод и 10 авторов](09.md)
- Готово (аудит; остатки v1 зафиксированы): [10. Сверка v1 и документации](10.md)
- Готово: [11. Работа с персонажами в проекте](11.md)

Выявленные при измерении остатки, вне текущего поручения 08–11:
- Отложена бессрочно, не исполнять: [12. Уменьшить стоимость рассылки графа](12.md).
- Отложена бессрочно, не исполнять: [13. Отзывчивость и память большой доски](13.md).

**Результат измерения 1000/10: бюджеты не достигнуты.** Задачи 12/13 и повторная проверка отложены бессрочно пользователем; это известное ограничение, не активная очередь работ.

Задачи 01–10 реализовали основной объём и выполнили аудит v1; аудит сохранил явные остатки в матрице, а измерение выделило задачи 12/13. Задача 11 — улучшение работы с персонажами в интерфейсе проекта. Порядок зависимостей — план выполнения, а не утверждение технической невозможности параллельной работы.

## Завершённая задача 01

Реализация завершена и проверена на настоящей PostgreSQL 17 и Microsoft Edge.

Изменено:
- src/server/creation.ts и миграция 005: журнал результата по scope + UUID, транзакционная блокировка конкурентного повтора, 409 при изменённом теле.
- app.ts / live.ts: Idempotency-Key для создания нод, диалогов, персонажей; доступ проверяется до повтора, рассылка только для нового результата после COMMIT. Старые запросы без ключа сохраняют прежнее поведение.
- api.ts: запись намерения в IndexedDB до HTTP, один немедленный сетевой повтор, сохранение неясного исхода и восстановление при открытии проекта/диалога; идентичное незавершённое намерение использует старый ключ.
- session.ts: восстановление HTTP-создания нод при инициализации, отдельно от очереди текста/графа. Ошибка восстановления показывает уведомление.
- tests/app.test.ts: конкурентный повтор трёх видов объектов, конфликт тела, проверка доступа, перезапуск и подсчёт объектов через API.
- tests/browser/collaboration.spec.ts: сервер принимает создание, оба HTTP-ответа теряются, перезагрузка повторяет прежний ключ; независимый браузер видит одну ноду.

Следующая задача: [02](02.md), персональная отмена структуры. Точки входа уже просмотрены: graph.ts применяет команды под блокировкой диалога; live.ts хранит только payload_hash в graph_operations. Для Undo нужны сохранённые обратные изменения и проверяемые версии полей, чтобы не отменять последующие правки коллег; сравнения одних координат недостаточно (чужое изменение могло вернуть те же координаты). Текст и preview не восстанавливать из старого снимка: поздний текст должен сохраниться. Начало защитить, связи восстанавливать только после проверки правил. Код задачи 02 в работе; см. текущий раздел ниже.

Дополнительные проверки: изоляция одинакового ключа между проектами и запрет доступа чужой сессии; повтор создания после удаления ноды не воскрешает её. Браузерные сценарии потери ответов и перезагрузки покрывают все три типа создаваемых объектов. Очередь HTTP привязана к nodic-tab, чтобы одинаковые намерения разных вкладок не объединялись.

Ограничения реализации: создание самого проекта не изменялось (ответ содержит секреты доступа); записи журнала пока не очищаются. Автоматический повтор после двух сетевых ошибок происходит при повторном открытии, интерфейс показывает ошибку. Для будущего удаления проектов/диалогов продумать очистку журнала и оставшихся клиентских намерений. Изменения не закоммичены.

## Проверки и окружение

- npm run typecheck и npm run build прошли. Vite предупреждает о крупном чанке ~873 kB (задача 09).
- Vitest: 11/11 прошли с настоящей PostgreSQL 17.4.
- Playwright: 6/6 прошли в Microsoft Edge, включая независимые браузерные контексты.
- git diff --check: без ошибок.
- Рабочее дерево содержит все изменения задачи 01 и документы задач; коммит пока не создавался.
- Обычный exec_command падает до запуска оболочки (Windows CET). Чтение/запуск доступны через node_repl с node:fs/promises и child_process.execFile.
- Docker недоступен в этой sandbox-сессии. Для проверок создан отдельный временный кластер C:/Temp/nodic-test-pg установленными C:/Program Files/PostgreSQL/17/bin/initdb.exe и postgres.exe. База nodic, роль nodic, localhost:55432, trust только в этом тестовом кластере. Это не пользовательские данные.
- pg_ctl start не работает с sandbox-токеном; postgres.exe запускается напрямую с -D C:/Temp/nodic-test-pg -h 127.0.0.1 -p 55432 через скрытый child_process.spawn.
- tsx падает при os.userInfo() в sandbox. Для тестового процесса использован C:/Temp/nodic-test-user.cjs (перехватывает только ERR_SYSTEM_ERROR у os.userInfo и возвращает username CodexSandboxOffline). Запуск API: node --require C:/Temp/nodic-test-user.cjs --import tsx src/server/main.ts. Production-код и зависимости ради этого не менялись.
- Vite запущен обычным node node_modules/vite/bin/vite.js --host 127.0.0.1; Playwright использует готовый сервер.
- Для Playwright задан PLAYWRIGHT_CHANNEL=msedge, SystemRoot=C:/Windows, SystemDrive=C:, PROGRAMFILES=C:/Program Files и PROGRAMFILES(X86)=C:/Program Files (x86). Без SystemDrive поиск Edge ошибочно использует undefined. В обычной пользовательской оболочке достаточно PLAYWRIGHT_CHANNEL=msedge.
- Git требует параметр -c safe.directory=E:/Projects/nodic на каждый вызов; глобальная конфигурация не изменялась.
- gh не авторизован; GitHub Issues ещё не опубликованы. После восстановления доступа проверить существующие Issues, сопоставить карточки и опубликовать недостающие через gh --body-file; сохранить ссылки без дублей.

Тестовые API/Vite остановлены после проверки. Временный кластер PostgreSQL остановлен через pg_ctl; его данные оставлены для следующего запуска.

## Завершённая задача 02

Задача 01 закоммичена: 1c77aae. Добавлен черновик history.ts и миграция 006: обратные операции, версии полей, привязка к сессии доступа. Интегрированы live.ts / создание нод и отдельные кнопки структурной Undo/Redo (общая история с текстом — задача 03). Проверки конфликтов, восстановления, повторов и атомарных групп завершены; результаты ниже.

### Продолжение 2026-09-15

На старте рабочее дерево чистое, HEAD ec9dfba; реализация истории и её основные тесты уже в коммите 22ad34d.

- Исправлен src/server/history.ts: восстановление ноды фиксирует актуальную ревизию текста для следующей обратной операции. Раньше Redo удаления ошибочно блокировался текстом коллеги, который уже существовал при восстановлении. Новые чужие правки после восстановления по-прежнему блокируют удаление.
- tests/app.test.ts: поздний текст при удалении теперь отправляет второй автор с отдельной сессией. Тест воспроизвёл ошибку на повторном удалении и прошёл после исправления; проверяет также повторные циклы восстановления и пропуск недопустимых связей.
- tests/browser/collaboration.spec.ts: добавлены Redo удаления ноды с текстом коллеги, повторное восстановление и отказ Redo после новой чужой правки.
- Проверка типов, 14/14 серверных тестов и production-сборка прошли. Предупреждение Vite о чанке ~875 kB остаётся для задачи 09.
- Playwright: 8/8 прошли в независимых контекстах Chrome после перезапуска сервера с исправлением. git diff --check прошёл. Тестовые API/Vite остановлены после проверки; контейнер PostgreSQL оставлен запущенным.
- Текущее окружение — Linux: Node доступен в /home/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node; npm отсутствует в PATH. Проверки выполняются напрямую через CLI из node_modules (typescript/bin/tsc --noEmit, vitest/vitest.mjs run, @playwright/test/cli.js test, vite/bin/vite.js build). PostgreSQL запущен через docker compose up -d --wait postgres; для Docker, подключения к БД и браузерных тестов потребовался запуск вне sandbox. Браузер — PLAYWRIGHT_CHANNEL=chrome. Старые Windows-инструкции выше относятся к прошлой сессии.
- GitHub Issues недоступны: gh issue list возвращает ошибку подключения к api.github.com; карточки не публиковались.

### Закрытие задачи 02 — 2026-09-15

- Серверный тест выполняет перемещение, Undo/Redo и повторы прежних идентификаторов между тремя перезапусками приложения. История и подтверждения сохраняются; повтор старого Undo после Redo не меняет граф. Все утверждения — через публичный API/WebSocket.
- Браузерный тест теряет первое подтверждение каждого из трёх обратных действий, разрывает соединение и проверяет повтор с тем же идентификатором, итоговый граф у независимого автора и состояние кнопок истории. Каждая операция отправлена ровно дважды.
- Итог: 15/15 серверных тестов, 9/9 браузерных тестов Chrome, TypeScript, production-сборка и git diff --check прошли. Первый браузерный запуск остановлен из-за неготового API (ECONNREFUSED); полный повтор после готовности API прошёл. При проверках запускать браузерный сервер после серверного набора и дождаться сообщения Nodic API.
- README.md и docs/first-slice.md отражают готовую структурную отмену. Клиентские стеки живут в текущем открытом диалоге и сбрасываются при его переключении/перезагрузке; серверная история и очередь неподтверждённых команд сохраняются. Единая история с текстом — задача 03.
- Тестовые API/Vite остановлены; PostgreSQL оставлен запущенным. Изменения не закоммичены. GitHub-карточка не опубликована из-за недоступного подключения, статус зафиксирован здесь.

Следующая задача: [03. Единая история текста и структуры](03.md). Начать с src/client/session.ts и TextEditor.tsx: объединить хронологию Yjs UndoManager и подтверждённых структурных действий текущей вкладки, затем подключить общие кнопки и Ctrl+Z/Redo. Проверить последовательность текст → перемещение → другой текст с двумя авторами и конфликт обратной команды.


## Завершённая задача 03 — 2026-09-15

Задача 02 закоммичена по просьбе пользователя: 81dc764 (Complete structural undo recovery and verification).

Начата реализация:
- src/client/session.ts: единые стеки текстовых и структурных действий; текстовые шаги регистрируются по событиям Yjs UndoManager, структурные — при постановке команды в очередь, чтобы позднее подтверждение не переставляло их после ввода. Границы структурных действий прерывают группировку текста; новое действие очищает Redo всех редакторов. Отклонённая команда удаляется из истории.
- src/client/App.tsx и TextEditor.tsx: общие кнопки «Отменить»/«Повторить», обработка Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z и Ctrl/Cmd+Y независимо от фокуса доски/CodeMirror. Обычные поля форм оставляют свою нативную отмену. Ввод при ожидающей структурной обратной команде приостановлен.
- tests/browser/collaboration.spec.ts: существующие сценарии переведены на общие кнопки; добавлен сценарий двух авторов «текст реплики → перемещение реплики → текст варианта», Undo/Redo из первого редактора и сохранение дописанного коллегой текста.
- Проверка типов, production-сборка и 15/15 серверных тестов прошли. В полном браузерном запуске прошли 9 существующих сценариев; новый смешанный сценарий прошёл отдельным запуском после исправления теста (перетаскивать видимую реплику, проверять текст в preview без меток курсоров). git diff --check прошёл. Предупреждение о чанке ~876 kB остаётся. Тестовые API/Vite остановлены, PostgreSQL оставлен запущенным.

### Закрытие задачи 03

- Текстовые шаги ссылаются на конкретный элемент стека Yjs. При Undo/Redo старые элементы временно отделены и возвращаются после вызова: пустой шаг после чужого удаления не перескакивает через структурное действие к более раннему вводу.
- HTTP-создание резервирует место в истории до запроса; ответ заполняет идентификатор операции на прежнем месте. Пока исход неизвестен, Undo/Redo недоступны. Структурные команды также регистрируются до подтверждения. Во время структурной обратной команды ввод и новые команды доски приостановлены.
- Браузерные проверки покрывают задержанное WS-подтверждение движения до ввода в другой ноде, задержанный HTTP-ответ создания до следующего ввода, пустую отмену после чужого удаления, конфликт движения, сохранение чужого текста и сброс Redo новым вводом.
- Итоговая проверка: TypeScript и production-сборка прошли; 15/15 серверных тестов и полный набор 11/11 браузерных тестов Chrome прошли. git diff --check без ошибок. Предупреждение Vite о чанке ~877 kB остаётся для задачи 09.
- Время жизни сверено с docs/specs/nodic-v1.md: повторное открытие не восстанавливает историю. Смена редактора/фокуса в открытом диалоге сохраняет её; переход по ссылке диалога сейчас открывает новую страницу. История между такими открытиями не переносится. README.md и docs/first-slice.md обновлены.
- Тестовые API/Vite остановлены; PostgreSQL оставлен запущенным. Изменения задачи 03 не закоммичены.

Следующая задача: [04. Копирование, вставка и групповое удаление](04.md). Начать с правил группы в src/server/graph.ts и формата GraphCommand: новые идентификаторы и независимые тексты при вставке, атомарность и один шаг Undo, защита начала. Определить вставку между диалогами одного проекта и проверить через публичные операции и независимые браузерные контексты.


## Завершённая задача 04 — 2026-09-15

Задача 03 закоммичена: 89c1e37 (Unify text and graph undo history). По поручению пользователя каждый проверенный логический блок теперь коммитится.

Первый блок — групповое удаление:
- protocol.ts / graph.ts: delete-nodes до 1000 идентификаторов, проверка всей группы до изменений; начало, удалённая/чужая нода и дубликат отклоняют всю команду. Ноды скрываются, внутренние и внешние инцидентные связи удаляются в одной транзакции. Существующая история записывает один шаг для всей группы.
- App.tsx: Shift+клик выделяет несколько нод, контекстное меню удаляет выделенную группу, Delete/Backspace удаляют выделение. Начало исключается из пользовательского выделения для удаления; сервер запрещает запросы с началом. Поля ввода и модальные редакторы защищены от горячей клавиши удаления доски.
- Серверный тест: атомарный отказ для начала/неизвестной ноды/дубликата, восстановление группы со связями одним Undo, повтор доставки после восстановления, Redo удаления.
- Браузерный тест: независимый автор видит удаление/восстановление группы, проверяются меню, клавиатура и защита начала.
- Проверка типов, production-сборка, 16/16 серверных тестов и полный набор 12/12 браузерных тестов Chrome прошли. git diff --check без ошибок. Тестовые API/Vite остановлены; PostgreSQL оставлен запущенным. Блок группового удаления коммитится отдельно, задача 04 остаётся в работе до копирования/вставки.

Следующий блок — копирование/вставка: сохранять снимок полного подтверждённого текста и только внутренних связей, исключать начало и связи с ним. При вставке назначать новые идентификаторы и создавать независимые Yjs-документы. Вставку разрешить между диалогами одного проекта с сохранением ссылок на персонажей; перенос в другой проект отклонять. Группа должна применяться атомарно и попадать в единую историю одним шагом.


### Закрытие задачи 04

Групповое удаление закоммичено ранее: 764a264. Копирование/вставка завершаются отдельным коммитом этого продолжения.

- app.ts: POST /api/dialogues/:dialogueId/copy проверяет доступ, блокирует граф и выбранные ноды на время получения снимка, возвращает полный текст и только внутренние связи без начала.
- protocol.ts / graph.ts: paste-nodes с фрагментом группы. Проверка проекта, уникальности идентификаторов, внутренних концов связей и персонажей; создание независимых Yjs-документов из строк. Все вставки и проверки связей выполняются в существующей транзакции live.ts, история и подтверждение сохраняются до рассылки. Повторы операции идемпотентны, Undo/Redo действует на всю группу.
- clipboard.ts / App.tsx: буфер sessionStorage текущей вкладки, Ctrl/Cmd+C и Ctrl/Cmd+V вне текстовых полей/модальных окон, меню копирования/вставки, сдвиг группы и формы кривых. Правила переноса и ограничения описаны в docs/tasks/04.md.
- Серверный тест проверяет снимок полного текста длиннее preview, исключение начала и внешней связи, доступ, перенос между диалогами одного проекта, отказ другого проекта, атомарный откат недопустимой связи, персонажа и изгиб, независимость текстов, повтор доставки до/после Undo и Redo.
- Браузерный тест с независимым автором проверяет копирование связанной ветки, изменение исходника после снимка, вставку в новый диалог, Undo/Redo всей группы и независимое редактирование копии.
- Итог: TypeScript, production-сборка, 17/17 серверных тестов и полный набор 13/13 браузерных тестов Chrome прошли. git diff --check прошёл. В промежуточном браузерном прогоне прежний тест движения не дождался сохранения во время обновления клиентского кода; полный повтор на неизменяемой версии прошёл. Не редактировать клиентские исходники во время e2e-прогона. Предупреждение Vite о чанке ~879 kB остаётся для задачи 09.
- README.md и docs/first-slice.md обновлены. Тестовые API/Vite остановлены; PostgreSQL оставлен запущенным.

Следующая задача: [05. Справочник персонажей](05.md). Начать с отдельной страницы проекта и серверных операций переименования/удаления. Учесть конкурентное назначение удаляемого персонажа и очистку ссылок во всех диалогах без потери текста. Проверить двух авторов в разных диалогах и изоляцию проектов.


## Завершённая задача 05 — 2026-09-15

Задача 04 закоммичена: 9789ff2. Задача 05 завершается отдельным коммитом текущего продолжения.

- Characters.tsx / App.tsx: страница /p/:projectId/characters по ссылке из боковой панели. Создание, сохранение имени/цвета и удаление с подтверждением. Справочник получает уведомления через подключение к первому диалогу проекта; переподключение обновляет список. При отсутствии соединения изменения отключены.
- app.ts: переименование редактором и идемпотентное удаление после проверки доступа. Удаление блокирует диалоги проекта в порядке идентификаторов, затем персонажа (тот же порядок, что у назначения), обновляет версии полей и очищает ссылки в видимых и скрытых нодах, затем удаляет персонажа в одной транзакции. Тексты не изменяются.
- live.ts: обновлённые графы и справочник рассылаются после COMMIT через очереди диалогов. Назначение, пришедшее после удаления, отклоняется; принятое ранее очищается удалением.
- Миграция 007 и characterMissing: реплика с удалённым персонажем показывает «Неизвестный персонаж», отличаясь от намеренно пустого назначения. Можно явно выбрать «Без персонажа». История сохраняет этот признак и читает старые записи, где хранился только UUID/null. Копирование/вставка также сохраняет признак неизвестного персонажа.
- Серверные проверки: два автора в двух диалогах, переименование, конкурентное назначение/удаление, запрет доступа чужому проекту, сохранение текста, повтор удаления, отказ старого Undo и восстановление неизвестного персонажа через Undo нового назначения.
- Браузер: создание через справочник, изменение имени и цвета у двух авторов в разных диалогах, удаление с сохранением открытых текстов, обновление выбора и повторное открытие конкретного диалога. В первом запуске тест перезагружал корень проекта, где первый диалог меняется из-за сортировки UUID; исправлен тест переходом по конкретной ссылке. Остальные 13 сценариев прошли в полном запуске, исправленный новый сценарий прошёл отдельно. Итого проверены все 14 сценариев.
- Итог: TypeScript, production-сборка и 18/18 серверных тестов прошли; git diff --check без ошибок. Страница визуально проверена по test-results/character-catalogue.png. Предупреждение Vite о чанке ~884 kB остаётся для задачи 09. README.md и docs/first-slice.md обновлены. API/Vite остановлены, PostgreSQL оставлен запущенным.

Следующая задача: [06. Управление проектами и диалогами](06.md). Добавить переименование и удаление с проверкой ролей, уведомлением подключённых авторов и обновлением недавних проектов. Определить удаление последнего диалога. Учесть новое подключение справочника к первому диалогу: удаление этого диалога должно переподключить справочник либо перевести его к проектному каналу событий.


## Завершённая задача 06 — 2026-09-15

Задача 05 закоммичена: 9d76b9b. Задача 06 завершается отдельным коммитом текущего продолжения.

- app.ts: переименование проектов/диалогов редактором; удаление только владельцем. Последний диалог защищён (409), конкурирующие изменения списка диалогов сериализуются блокировкой проекта. Удаление графа и журналов — одна транзакция, явное удаление связей предшествует каскаду нод. Старый журнал создания удалённого диалога сохраняется, чтобы повтор не воскрешал его; при удалении проекта очищаются все журналы его scopes.
- live.ts: события project-changed содержат имя проекта для недавних; после COMMIT removed доставляется в затронутые комнаты, затем соединения закрываются. Поздние операции проверяют доступ и не воскрешают удалённые данные.
- ManageProject.tsx / App.tsx: интерфейс переименования и подтверждения удаления, кнопки удаления доступны только владельцу; последний диалог защищён также интерфейсом. Поля имён реагируют на изменения коллег.
- project-events.ts / session.ts: остановка удалённой сессии, очистка очередей затронутых объектов, обновление недавних проектов и переход с уведомлением. Справочник после удаления своего диалога открывается заново по прежнему адресу и подключается к оставшемуся. При удалении проекта все подключённые страницы переходят на главную.
- Серверные тесты: запреты редактору через прямой API, разрешение переименования, запрет последнего диалога, открытый текст/операция в пути, уведомления, позднее создание ноды, повтор создания удалённого диалога и запрет запросов после удаления проекта.
- Браузер: владелец, редактор и отдельная страница справочника; переименование, удаление открытого диалога, сохранение подключения справочника, запрет последнего диалога, удаление проекта, уведомления, переходы и очистка недавних.
- Итог: TypeScript, production-сборка, 19/19 серверных тестов и полный набор 15/15 браузерных тестов Chrome прошли; git diff --check без ошибок. Предупреждение Vite о чанке ~888 kB остаётся для задачи 09. README.md и docs/first-slice.md обновлены. API/Vite остановлены, PostgreSQL оставлен запущенным.
- В задаче 08 отдельно проверить вкладку, которая была отключена всё время удаления и пропустила removed: обнаружение удаления при восстановлении доступа и очистку её очередей. Текущие проверки задачи 06 покрывают подключённых авторов и поздние запросы.

Следующая задача: [07. Присутствие авторов на доске](07.md). Добавить временную идентичность автора, список, курсоры и выделения; согласовать их с текстовым редактором. Очищать присутствие при выходе/смене диалога, не передавать секреты доступа. Проверки — два независимых браузера, движение, выделение, смена диалога и отключение.


## Завершённая задача 07 — 2026-09-15

Задача 06 закоммичена: 6508049. Задача 07 завершается отдельным коммитом текущего продолжения.

- protocol.ts / live.ts: отдельные временные сообщения присутствия, серверный идентификатор подключения, проверка публичных полей, список комнаты и очистка при отключении. Heartbeat 15 секунд удаляет потерянные подключения; состояние не сохраняется и не проходит через очередь транзакций. Подтверждения текста и графа остаются после COMMIT.
- author.ts / session.ts / TextEditor.tsx: общий профиль вкладки для доски и текста, изменение имени, ограничение отправки до одного обновления за 50 мс, очистка при отключении. Справочник Characters.tsx не публикует присутствие.
- App.tsx / StoryEdge.tsx / style.css: список авторов, курсоры в координатах графа и подписи чужих выделений нод/связей.
- Серверный тест проверяет отдельные идентификаторы подключений, невозможность подмены идентификатора, отсутствие секретов и лишних полей, выход автора и отсутствие присутствия после перезапуска. Браузерные проверки покрывают имена, движение, выделения, совпадение с текстовым редактором, отключение, возврат и переход между диалогами. Проверки используют публичные операции без SQL-утверждений.
- TypeScript, production-сборка и 20/20 серверных тестов прошли. В полном браузерном запуске прошли 15 из 16 сценариев; прежний тест удаления обнаружил гонку перехода после создания второго диалога. Добавлено ожидание окончания перехода; исправленный сценарий и новый сценарий присутствия прошли повторно (2/2). Все 16 сценариев проверены, единого полного зелёного прогона после исправления не было. git diff --check прошёл. Предупреждение Vite о чанке ~892 kB остаётся для задачи 09. API/Vite остановлены, PostgreSQL оставлен запущенным.

Следующая задача: [08. Отказы и восстановление](08.md). Проверить потерю подтверждения, перезапуск процесса, недоступность PostgreSQL и закрытие вкладки во время записи очереди; явно описать границу гарантии до завершения IndexedDB. Отдельно обработать вкладку, которая была отключена всё время удаления проекта/диалога и пропустила removed: обнаружение удаления при переподключении и очистку очередей.

## Завершённая задача 08 — 2026-09-15

Исходный HEAD: 33440cf. Задача завершается отдельным коммитом по поручению пользователя.

- `src/server/database.ts`: обработаны ошибки соединения на idle и выданных клиентах PostgreSQL. Управляемый разрыв воспроизвёл аварийное завершение API на событии error; теперь процесс остаётся жив, запрос отклоняется без ACK, в stderr выводится сообщение без параметров подключения.
- Миграция 008, `app.ts`: удаление транзакционно сохраняет квитанции для действующих сессий. `/api/projects/:id/removals` выдаёт только идентификаторы удалённых объектов обладателю соответствующей исходной cookie до её прежнего срока истечения. После удаления проекта и перезапуска квитанции доступны; чужая/поддельная/отсутствующая cookie отклоняется.
- `project-events.ts`, `api.ts`, `session.ts`: при повторном открытии и неудачном переподключении проверяются квитанции. Вкладка, пропустившая removed, очищает соответствующие WebSocket/HTTP-очереди и покидает объект; 403/503 и сетевые ошибки ничего не стирают. Повторный online не создаёт второй сокет во время CONNECTING.
- `tests/recovery.test.ts`: реальный PostgreSQL за TCP-прокси, удержание COMMIT до отправки в БД и публичное чтение старого состояния без ACK/трансляции; разрыв всех соединений до COMMIT, повтор, потеря ACK, SIGKILL отдельного процесса API, восстановление текста и повтор прежнего движения после нового без отката позиции. SQL-утверждений о таблицах нет.
- `tests/browser/recovery.spec.ts`: 5 сценариев в независимых контекстах Edge. Офлайн-удаление проекта/диалога очищает неподтверждённый текст и намерение HTTP-создания; потерянный текстовый ACK повторяется тем же UUID после закрытия/восстановления вкладки; незавершённая настоящая IndexedDB-транзакция не допускает отправки и статуса «Сохранено» до закрытия; 403/503 сохраняют очередь для успешного повтора.
- README, рабочий срез и карточка 08 описывают границу гарантии: до завершения IndexedDB правка может потеряться, после неё требуется та же вкладка с sessionStorage и сохранное браузерное хранилище. Новая самостоятельная вкладка не забирает старую очередь. Квитанции не помогают после истечения исходной сессии и для удалений до миграции 008; в этих случаях очередь остаётся. Потеря питания/диска PostgreSQL и аварийное завершение всего браузера не моделировались.

Проверки: `npm run typecheck`, `npm test` — 22/22, `npm run test:e2e` — 21/21 Microsoft Edge, `npm run build`, `git diff --check` прошли. Vite предупреждает о чанке 892.76 kB (279.19 kB gzip): измерение нагрузки и необходимости оптимизации — задача 09.

### Текущее Windows-окружение для следующих задач

- `exec_command`: `shell: "cmd.exe", login: false`; PowerShell в этой сессии падает до исполнения (CET). Для Git каждый раз `git -c safe.directory=E:/Projects/nodic …`. Глобальные настройки не менялись.
- `npm run db:up` проверен: Docker daemon не запущен. Использован установленный PostgreSQL 17.4, отдельный тестовый кластер `E:/Projects/nodic/.task08-pg`, база `nodic` UTF8, роль `nodic`, порт `55432`, trust только в локальном тестовом кластере. Директория имеет локальный игнорирующий все файлы `.gitignore`, не входит в коммит. Перемещение остановленного каталога в `.scratch` отклоняется Windows EPERM; кластер оставлен на исходном месте.
- PostgreSQL оставлен запущенным. Повторный запуск после остановки: `C:\Progra~1\PostgreSQL\17\bin\postgres.exe -D E:\Projects\nodic\.task08-pg -h 127.0.0.1 -p 55432`. Остановка: `C:\Progra~1\PostgreSQL\17\bin\pg_ctl.exe -D E:\Projects\nodic\.task08-pg -m fast stop`.
- Обход `os.userInfo()` для tsx лежит в игнорируемом `.scratch/task08-env.cjs`. Серверные проверки: `set NODE_OPTIONS=--require=./.scratch/task08-env.cjs&&npm test`. Обход наследуется отдельным API-процессом нового теста; production-код не зависит от файла.
- API запускается `node --require ./.scratch/task08-env.cjs --import tsx src/server/main.ts`, Vite — `node node_modules/vite/bin/vite.js --host 127.0.0.1`. API/Vite оставлены запущенными на 3001/5173. После серверных изменений API перезапустить перед e2e.
- Браузерные проверки: `set PLAYWRIGHT_CHANNEL=msedge&&set SystemRoot=C:\Windows&&set SystemDrive=C:&&set PROGRAMFILES=C:\Program Files&&set PROGRAMFILES(X86)=C:\Program Files (x86)&&npm run test:e2e`. Typecheck/build работают обычными npm-командами.

Следующая задача: [09. Нагрузка 1 000 нод и 10 авторов](09.md). Задача 09 этим исполнителем не начиналась.
